specVersion: "3.1"
metadata:
  name: v2lrop0909
type: application
server:
  customMiddleware:
    - name: fiori-tools-preview
      afterMiddleware: compression
      configuration:
        flp:
          theme: sap_horizon
          path: flpSandbox.html
          intent:
            object: Chicken
            action: dance
          libs: true
