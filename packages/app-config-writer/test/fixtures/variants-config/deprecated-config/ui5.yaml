specVersion: "3.1"
metadata:
  name: v2lrop0909
type: application
server:
  customMiddleware:
    - name: fiori-tools-preview
      afterMiddleware: compression
      configuration:
        component: v2lrop0909
        ui5Theme: sap_horizon
# Test variants-config with no fiori-tools-preview middleware config and missing delay for fiori-tools-appreload