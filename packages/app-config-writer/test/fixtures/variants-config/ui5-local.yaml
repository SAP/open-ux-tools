specVersion: "3.1"
metadata:
  name: v2lrop0909
type: application
server:
  customMiddleware:
    - name: fiori-tools-proxy
      afterMiddleware: compression
      configuration:
        ignoreCertError: false
        backend:
          - path: /sap
            url: https://sap-ux-mock-services-v2-lrop.cfapps.us10.hana.ondemand.com
    - name: fiori-tools-preview
      afterMiddleware: fiori-tools-appreload
      configuration:
        component: v2lrop0909
        ui5Theme: sap_horizon
# Test variants-config with a deprecated fiori-tools-preview middleware config and no fiori-tools-appreload
