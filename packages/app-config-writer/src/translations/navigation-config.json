{
    "error": {
        "sapAppNotDefined": "The `manifest.json` file is missing the `sap.app` required section.",
        "manifestNotFound": "The `manifest.json` file was not found at path: {{- path}}. Check the file exists.",
        "inboundExists": "An inbound definition already exists for key: {{inboundKey}}. Choose another key."
    }
}