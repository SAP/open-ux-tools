import { CustomPage } from './types';

/**
 * Enhances the provided custom page configuration with default data
 *
 * @param {CustomPage} data - a custom page configuration object
 */
export function enhanceData(data: CustomPage): void {
    // TBD
}
