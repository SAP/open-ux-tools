sap.ui.define(['sap/ovp/app/Component'], (OvpAppComponent) => {
    'use strict';

    return OvpAppComponent.extend('ovp.app.Component', {
        metadata: {
            manifest: 'json'
        }
    });
});
