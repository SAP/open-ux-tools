// import type { manifest, VSCodeManifest, modules } from './types';

export class TelemetrySystem {
    // static WORKSTREAM: string;
    static telemetryEnabled: boolean = true;
    // static manifest: manifest | VSCodeManifest;
}

