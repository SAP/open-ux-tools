// Type definitions for @sap/ux-cds-compiler-facade under NodeNext module resolution
// The @sap/ux-cds-compiler-facade package doesn't have proper exports, so we need to declare types here

declare module '@sap/ux-cds-compiler-facade/dist/metadata' {
    export interface MetadataElementWithParentKey {
        [key: string]: any;
    }
}
