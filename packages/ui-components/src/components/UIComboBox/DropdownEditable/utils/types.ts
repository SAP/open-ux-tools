export const EDITABLE_ENTRY_PREFIXES = ['zz_new']; // TODO reuse PREFIX_NEW from '@sap/ux-fiori-ai-project-generator' ?

// Editable Tree Dropdown - temporary prefix to avoid style colissions while we have two versions
export const CLASS_NAME_PREFIX = 'etd';
