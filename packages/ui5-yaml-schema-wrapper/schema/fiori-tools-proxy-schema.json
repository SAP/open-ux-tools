{
    "description": "Configuration for fiori-tools-proxy middleware. This middleware combines UI5 proxy and backend proxy functionality.",
    "type": "object",
    "properties": {
        "backend": {
            "description": "Backend proxy configurations.",
            "type": "array",
            "items": {
                "$ref": "backend-proxy-middleware-schema.json#/definitions/BackendConfig"
            }
        },
        "ui5": {
            "description": "UI5 proxy configuration.",
            "$ref": "ui5-proxy-middleware-schema.json#/properties/ui5"
        },
        "proxy": {
            "description": "If set then it will override the proxy settings from node.",
            "type": "string"
        },
        "ignoreCertError": {
            "deprecated": "Use ignoreCertErrors (plural) instead.",
            "type": "boolean"
        },
        "ignoreCertErrors": {
            "description": "If set to true, certificate errors will be ignored (e.g., self-signed certificates).",
            "type": "boolean"
        },
        "debug": {
            "description": "Enable debug logging.",
            "type": "boolean"
        },
        "bsp": {
            "description": "The BSP property for the FLP Embedded Flow. The property refers to the BSP Application Name.",
            "type": "string"
        }
    },
    "additionalProperties": false
}
