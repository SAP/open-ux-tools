<core:FragmentDefinition xmlns:core="sap.ui.core" xmlns="sap.m" xmlns:l="sap.ui.layout">
	<VBox width="<%= width %>" <%- handler ? ' core:require="{handler: \'' + handler + '\'}"' : '' %>>
		<SegmentedButton id="<%= id %>" selectionChange="handler.onSelectionChange" enabled="<%= enabled %>">
			<items>
			<% items.forEach(function(item) { %>
				<SegmentedButtonItem text="<%= item.text %>" />
			<% }); %>
			</items>
		</SegmentedButton>
	</VBox>
</core:FragmentDefinition>