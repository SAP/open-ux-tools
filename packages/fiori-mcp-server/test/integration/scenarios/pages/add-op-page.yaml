config:
    project: 'node-ai-created'
vars:
    appPath: '{{APP_PATH}}'
    query: 'Add Expenses object page to parent page TravelsObjectPage. Application in {{appPath}}'
assert:
    - type: javascript
      value: file://../assertions/snapshots.ts:validate
      config:
          file: webapp/manifest.json
          snapshot: add-op-page
