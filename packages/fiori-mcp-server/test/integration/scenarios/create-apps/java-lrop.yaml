config:
    project: 'java-ai-created'
vars:
    appPath: '{{PROJECT_PATH}}'
    query: |
      Create an new application to manage expenses in {{appPath}}. It MUST be based on List Report Object Page floor plan.
      Note: You MUST not asks for any additional information.
assert:
    - type: javascript
      value: file://../assertions/projectStructure.ts:validate
