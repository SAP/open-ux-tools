config:
    project: 'lrop'
vars:
    appPath: '{{PROJECT_PATH}}'
    query: 'Add object page to BookingObjectPage in application with appPath: {{appPath}}'
assert:
    - type: javascript
      value: file://../assertions/snapshots.ts:customAssert
      config:
          file: webapp/manifest.json
          snapshot: add-op-page
