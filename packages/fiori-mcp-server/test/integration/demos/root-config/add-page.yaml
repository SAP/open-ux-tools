
vars:
    project: 'lrop'
    appPath: '{{PROJECT_PATH}}'
    pageType: ObjectPage
    parent: BookingObjectPage
    query: 'Add {{pageType}} page to {{parent}} in application with appPath: {{appPath}}'
assert:
    - type: javascript
      value: file://../../assertions/snapshots.ts:customAssert
      config:
        file: webapp/manifest.json
        snapshot: add-page
