---
"@sap-ux/fiori-mcp-server": patch
---

FIX: `download_odata_service_metadata` returns a structured error when `appPath` does not exist, and clarifies the pre-condition in the parameter description so LLMs know to create the directory first
