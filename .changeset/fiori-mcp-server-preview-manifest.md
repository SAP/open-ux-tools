---
"@sap-ux/fiori-mcp-server": minor
---

FEAT: add preview_manifest tool that validates an Adaptation Project's descriptor (manifest) changes by delegating to previewManifest from @ui5/task-adaptation, producing the merged manifest.json on the fly without a full build (Cloud Foundry ADP projects only)
