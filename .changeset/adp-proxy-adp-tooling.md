---
"@sap-ux/adp-tooling": patch
---

FIX: Route UI5 version validation and version-list requests through the proxy-aware HTTP layer so the Adaptation Project generator works behind a corporate proxy (no longer fails with HTTP 400 / "version not compatible")
