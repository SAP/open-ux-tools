---
'@sap-ux/generator-adp': patch
---

FIX: sonar fix for `Review this equality assertion: the compared expressions have incompatible static types ("string" and "number").` related to method `getFirstArgAsString`
