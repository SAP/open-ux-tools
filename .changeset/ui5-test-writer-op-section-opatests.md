---
"@sap-ux/ui5-test-writer": patch
---

FIX: Generate a separate, named opaTest block for each Object Page section instead of one combined "Check body sections" block, so large object pages produce readable, independently-failing journey tests
