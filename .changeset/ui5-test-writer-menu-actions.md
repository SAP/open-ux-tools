---
'@sap-ux/ui5-test-writer': minor
---

FEAT: Generate Object Page OPA tests for menu (drop-down) actions. Actions grouped in an annotation menu (`DataFieldForActionGroup`) or a manifest menu (`CustomMenu`) are now detected from the specification model and emitted with the menu-aware `iCheckMenuAction` / `iExecuteMenuAction` test API.
