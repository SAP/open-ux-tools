---
"@sap-ux/ui5-test-writer": minor
---

FEAT: Generate per-tab OPA5 tests for multi-table (Multiple Table Mode) List Reports — columns, toolbar actions, create/delete and contact-card checks now run on each non-custom tab, and object-page navigation is resolved to the tab that exposes the page (by entity set)
