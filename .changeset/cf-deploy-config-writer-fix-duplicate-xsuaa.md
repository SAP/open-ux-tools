---
"@sap-ux/cf-deploy-config-writer": patch
---

FIX: Standalone approuter no longer creates duplicate XSUAA resource when one already exists in mta.yaml; new XSUAA resources now use `-auth` suffix instead of `-uaa`
