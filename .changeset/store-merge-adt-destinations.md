---
"@sap-ux/store": minor
---

FEAT: Merge ADT HTTP destinations (reentrance-ticket) from ~/.adtls/destinations.json into the backend system store as ABAP-on-BTP systems, tagged with a runtime-only ADT origin; edits and deletes to these systems are routed back to destinations.json and never written to systems.json
