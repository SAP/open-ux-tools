---
'@sap-ux/odata-service-inquirer': minor
'@sap-ux/fiori-app-sub-generator': patch
---

Adds a system store warning to password input, add new prompt (default false) whether to store the credentials for new system / existing system on reauth and a relevant breadcrumb.
