---
"@sap-ux/project-access": patch
"@sap-ux/cap-config-writer": patch
---

fix: bump min `cds-plugin-ui5` version to `^0.17.0` to avoid welcome-page injection error reported with `^0.13.0` ("Failed to inject application pages to welcome page")
