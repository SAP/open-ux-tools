---
'@sap-ux/fiori-annotation-api': patch
---

fix: Fixed a bug where pointers were not generated correctly when annotations were applied to targets belonging to different services in the same CDS file.
