---
"@sap-ux/eslint-plugin-fiori-tools": minor
---

FEAT: Add rule sap-grouping-supported-table-types-only — warns when grouping is configured for table types that do not support it (GridTable, TreeTable)
