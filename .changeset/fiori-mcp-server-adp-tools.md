---
"@sap-ux/fiori-mcp-server": minor
---

FEAT: add ADP tools for adaptation project generation, controller extension authoring, RTA workflow navigation via Playwright, and UI5 control documentation lookup (`lookup_ui5_documentation` — resolves aggregation, property, and event docs from a library's api.json, walking the inheritance chain across libraries and targeting the app's pinned UI5 version via ui5.yaml or manifest.json)
